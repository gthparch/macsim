* How to patch
  All patches are based on 'svn diff'
  Please patch Macsim using this command
  -> patch -p0 -i xxx.patch


* How to build Macsim
  To build Macsim, autotool (xxx or higher) is required.

  aclocal
  automake --add-missing
  autoconf
  ./configure
  make

  or aclocal && automake --add-missing && autoconf && ./configure && make

  or ./autogen.sh

